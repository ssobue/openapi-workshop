<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.sobue.workshop.product.mybatis.mapper.ProductMapper">

    <resultMap id="productResultMap" type="dev.sobue.workshop.product.Product">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="currency" column="currency"
            typeHandler="dev.sobue.workshop.common.mybatis.typehandler.CurrencyTypeHandler"/>
        <result property="price" column="price"/>
        <result property="stock" column="stock"/>
    </resultMap>

    <select id="selectAll" resultMap="productResultMap">
        SELECT id, name, currency, price, stock
        FROM products
        ORDER BY id
    </select>

    <select id="selectById" parameterType="long" resultMap="productResultMap">
        SELECT id, name, currency, price, stock
        FROM products
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="dev.sobue.workshop.product.Product" useGeneratedKeys="true"
        keyProperty="id">
        INSERT INTO products (name, currency, price, stock)
        VALUES (#{name},
                #{currency, typeHandler=dev.sobue.workshop.common.mybatis.typehandler.CurrencyTypeHandler},
                #{price}, #{stock})
    </insert>

    <update id="update" parameterType="dev.sobue.workshop.product.Product">
        UPDATE products
        SET name     = #{name},
            currency = #{currency, typeHandler=dev.sobue.workshop.common.mybatis.typehandler.CurrencyTypeHandler},
            price    = #{price},
            stock    = #{stock}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE
        FROM products
        WHERE id = #{id}
    </delete>
</mapper>
