# Order schema
# Multi-file OpenAPI schema fragment

type: object
title: Order
properties:
  id:
    description: Order ID
    readOnly: true
    type: integer
    format: int64
  customerName:
    description: Customer Name
    type: string
    minLength: 1
    maxLength: 1000
  status:
    $ref: './OrderStatus.yaml'
  items:
    description: Order items
    type: array
    items:
      $ref: './OrderItem.yaml'

required:
  - customerName
